[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monarch-api2"
version = "0.1.0"
description = "Unofficial Monarch Money API client."
readme = "MONARCH_API_FUNCTION_PLAN.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
]
